<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Freedmoye - Blog{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
<div class="pt-5 container">
    <header class="d-flex justify-content-between align-items-center">
        <h1>⭐ <a href="{{ path('app_home') }}">Freedmoye</a></h1>

        <a href="{{ path('app_searched_posts_create') }}" class="btn btn-outline-primary">🔍 Rechercher</a>
    </header>

    <hr class="text-secondary">

    {{ include('layouts/partials/_flash_messages.html.twig') }}

    <div class="row">
        <main role="main" class="col-md-8">
            {% block body %}{% endblock %}
        </main>

        <aside class="col-md-4">
            {{ render_esi(controller(
                'App\\Controller\\PostController::featuredContent',
                { 'maxResults': 5 }
            )) }}
        </aside>
    </div>

    <footer class="my-5">
        <p class="text-muted">
            Construit avec <span class="text-danger">&hearts;</span> par <a href="https://freedmoye.com">Freedmoye</a>.
        </p>
    </footer>
</div>
</body>
</html>
